{# ============================================================================
   AGENTS.md.jinja — Codex 컨텍스트 파일 (하이브리드: managed 블록 + 보존 로컬)

   배포 방식: 하이브리드 (base-layout §5·§8). Codex는 @import를 지원하지 않아
   공유 코딩 규칙을 인라인해야 한다. managed 블록 안에서 .tack/rules/*를 Jinja
   include 지시어로 인라인하고, `copier update`가 이 블록을 source의 최신
   .tack/rules/로 재생성한다. 블록 밖 프로젝트 섹션(flow-init이 LLM으로 채운)은
   3-way merge로 보존 → 도구 skew 없는 freshness.

   managed 블록 마커 = `shared-rules` (T4.3 결정, CLAUDE.md.jinja와 통일).
   include 대상 = .tack/rules/ 전체 6개 규칙(coding-style·git-workflow·security·
   testing + typescript/patterns·typescript/testing). CLAUDE.md @import 집합과
   동일 세트를 보장한다 (base-layout §8, 규칙 집합 대칭). typescript/2가 Codex
   대상 언어 규칙은 아니지만, 여기서는 규칙 **집합**만 대칭으로 맞춘다 — TS 규칙
   본문을 tack 스택(Python)으로 교체하는 작업은 E7-S4 범위다.

   include 경로 주의: Copier의 Jinja loader searchpath는 `_subdirectory`가 아니라
   템플릿 clone 루트다. `_subdirectory: template`은 렌더·복사 대상 subtree만
   정하고 include loader 루트는 옮기지 않는다. 따라서 include 경로는 clone 루트
   기준(repo-root-relative)이어야 하며 `template/` 접두사가 필요하다 — 접두사가
   없으면 `copier copy`가 TemplateNotFound로 전체 렌더를 중단한다 (Copier 9.17
   실측 확인).

   include 대상 rule 파일 주의: 이 6개 파일(typescript/2 포함)은 standalone 복사 시
   verbatim(비-.jinja 미렌더)이지만 여기서 include되면 Jinja로 렌더된다. 향후 편집에서 `{{`·`{%`·`{#`
   raw 델리미터를 넣으면 AGENTS.md 렌더만 조용히 깨진다 (CLAUDE.md는 @import라
   무영향). 필요 시 `{% raw %}`로 감싸거나 델리미터를 피한다.
============================================================================ #}
# AGENTS.md

Codex CLI가 이 저장소에서 작업할 때의 안내 파일. Claude Code는 `CLAUDE.md`를 사용한다.

## 프로젝트 개요

<!-- flow-init이 프로젝트 이해 기반으로 채우는 슬롯 (Copier는 비워 둔다) -->

## 기술 스택

<!-- flow-init이 스택 추론으로 채우는 슬롯 (Copier는 비워 둔다) -->

<!-- shared-rules:begin -->
{% include 'template/.tack/rules/coding-style.md' %}

{% include 'template/.tack/rules/git-workflow.md' %}

{% include 'template/.tack/rules/security.md' %}

{% include 'template/.tack/rules/testing.md' %}

{% include 'template/.tack/rules/typescript/patterns.md' %}

{% include 'template/.tack/rules/typescript/testing.md' %}
<!-- shared-rules:end -->
